<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="@style/Theme.Leanback">
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowAnimationStyle">@style/AnimationActivity</item>
    </style>

    <style name="AnimationActivity" parent="@android:style/Animation.Activity">
        <!--用于设置打开新的Activity并进入新的Activity展示的动画：A 启动 B 时，B 出现在屏幕上，这时执行的是B的-->
        <item name="android:activityOpenEnterAnimation">@anim/anim_enter</item>
        <!--用于设置打开新的Activity并销毁之前的Activity展示的动画：A 启动 B 时，A 从屏幕上消失，这时执行的是A的-->
        <item name="android:activityOpenExitAnimation">@anim/anim_exit</item>
        <!--用于设置关闭当前Activity进入上一个Activity展示的动画：从 B 返回A时，A 出现在屏幕上，这时执行的是A的-->
        <item name="android:activityCloseEnterAnimation">@anim/anim_enter</item>
        <!--用于设置关闭当前Activity时展示的动画：从 B 返回A时，B从屏幕上消失，这时执行的是B的-->
        <item name="android:activityCloseExitAnimation">@anim/anim_exit</item>
    </style>
    <style name="ProgressBar_Mini" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:maxHeight">6dp</item>
        <item name="android:minHeight">6dp</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">
            @android:drawable/progress_indeterminate_horizontal
        </item>
        <item name="android:progressDrawable">@drawable/progressbar</item>
    </style>
</resources>
